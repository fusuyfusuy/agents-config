# Antigravity CLI and status runtime state, logs, auth, caches
*.log
*.tmp
*.jsonl
!.agents/activity.jsonl
*.db
quota-cache.json
status-state.json
oauth_creds.json
antigravity-oauth-token
google_accounts.json
projects.json
state.json
trustedFolders.json
installation_id
.DS_Store
__pycache__/
*.pyc
.venv/
*.egg-info/

# Generated by `agent-ctx map`/`init` — regenerate on demand, don't commit a
# snapshot that silently drifts from the real repo between regenerations.
.agents/repo_map.md

# Local backup files created during setup
*.bak*
